import { StyleSheet } from "react-native";
import { SIZES } from "../../constants/theme"

const style = StyleSheet.create({
    image: {
        resizeMode: "cover",
        width: SIZES.width,
        height: SIZES.height,
    }
})